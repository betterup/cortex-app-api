# frozen_string_literal: true

RSpec.describe Cortex do
  it 'has a version number' do
    expect(Cortex::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
